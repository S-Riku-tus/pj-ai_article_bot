name: Qiita Slack Notifier

on:
  schedule:
    - cron: "10 19 * * *"  # 毎日 08:30 JST (UTC の 23:30)

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: リポジトリをチェックアウト
        uses: actions/checkout@v4

      - name: Python をセットアップ
        uses: actions/setup-python@v4
        with:
          python-version: "3.9"

      - name: 依存関係をインストール
        run: pip install slack-sdk requests

      - name: Qiita 通知スクリプトを実行
        env:
          SLACK_TOKEN: ${{ secrets.SLACK_TOKEN }}
          CHANNEL_ID: "#最新記事タイムライン"  # ここを適切なチャンネルに変更
        run: python notify.py
